name: build_akcent
on:
  workflow_dispatch:

concurrency:
  group: build_akcent_${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Build AK-Cent feed
        env:
          AKCENT_XML_URL: ${{ secrets.AKCENT_XML_URL }}
          AKCENT_XML_PATH: docs/akcent_source.xml

          OUT_FILE: docs/akcent.yml
          OUTPUT_ENCODING: utf-8-sig
          HTTP_TIMEOUT: '60'

          # ФИЛЬТР: читаем ГОТОВЫЙ файл ключей из репозитория
          AKCENT_KEYWORDS_FILE: docs/akcent_keywords.txt
          AKCENT_KEYWORDS_MODE: "prefix"      # <-- только если НАЗВАНИЕ начинается с ключа
          # (опц.) файл исключений — строки, при наличии которых товар отбрасывается
          AKCENT_EXCLUDE_FILE: docs/akcent_exclude.txt
        run: |
          set -euo pipefail
          python scripts/build_akcent.py

      - name: Commit & push feed
        env:
          BRANCH_NAME: ${{ github.ref_name }}
        run: |
          set -e
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/akcent.yml docs/akcent_source.xml || true
          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi
          git commit -m "Update akcent.yml (prefix filter by akcent_keywords.txt)"
          git pull --rebase origin "$BRANCH_NAME" || git pull --no-rebase -X ours origin "$BRANCH_NAME"
          git push origin "$BRANCH_NAME"
