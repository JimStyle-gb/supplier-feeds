name: build_alstyle

on:
  workflow_dispatch: {}
  schedule:
    - cron: "15 */6 * * *"   # каждые 6 часов со сдвигом

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          pip install requests

      - name: Build Alstyle feed (YML only, real availability)
        shell: bash
        env:
          SUPPLIER_NAME: alstyle
          # ✅ официальный экспорт Alstyle — уже зашит в сам скрипт, но дублируем в env для явности:
          SUPPLIER_URL: https://al-style.kz/upload/catalog_export/al_style_catalog.php
          OUT_FILE: docs/alstyle.yml
          OUTPUT_ENCODING: windows-1251

          TIMEOUT_S: "30"
          RETRIES: "4"
          RETRY_BACKOFF_S: "2"
          MIN_BYTES: "1500"

          VENDORCODE_PREFIX: AS
          VENDORCODE_CREATE_IF_MISSING: "1"

          # Ключевой фильтр по <name> (если нужно — включай)
          ALSTYLE_KEYWORDS_PATH: docs/alstyle_keywords.txt
          ALSTYLE_KEYWORDS_MODE: off   # off|include|exclude
        run: |
          set -euo pipefail
          python scripts/build_alstyle.py
          mkdir -p docs
          : > docs/.nojekyll

      - name: Commit & Push
        shell: bash
        run: |
          set -euo pipefail
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name  "github-actions[bot]"
          git add docs/alstyle.yml docs/.nojekyll || true
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "build: alstyle YML (real availability, official source)"
            git push
          fi
